# 效能與畫質診斷報告（2026-07-05）

> 針對「PC 上 60fps 仍不順」「PC build 畫面粗糙」「UI 髒/顆粒大」「素材尺寸該怎麼訂」四個問題的完整調查。
> 結論先講：**四個問題都找到了明確原因，且都不需要換美術風格**。

---

## 0. 根因總表

| 症狀 | 根本原因 | 修法（見 §5） |
|---|---|---|
| fps 60 或更高仍不順 | 物理 50Hz（Fixed Timestep 0.02）×螢幕 60Hz 拍頻 + Player/Monster 的 Rigidbody2D **內插(Interpolate)全關** → 角色每秒約 10 次「跳半格」 | 開 Interpolate + Fixed Timestep 改 1/60 |
| 解 VSync 後 fps 狂升仍不順 | KVM 輸出仍是 60Hz，>60fps 畫面根本顯示不出來，只會撕裂＋節奏更亂；不順的源頭是上一列，不是 fps 不夠 | 同上，且 VSync 建議開著 |
| 世界畫面粗糙 | 地圖素材 256px/格，1080p 下一格只顯示 **108px**，等於 **0.42 倍縮小**，又用 **Point 濾波、無 mipmap** → 六成像素被隨機丟棄，產生噪點與移動閃爍 | 場景貼圖改 Bilinear + 開 mipmap |
| 編輯器裡卻不覺得粗糙 | Mac Retina 的 Game view 實際渲染像素密度高（接近 1:1 取樣），PC 1080p 才會掉到 0.42x | 同上（修完後兩邊一致） |
| UI 髒、顆粒大 | **不是風格問題**。icon/按鈕原圖 256~500px，實際顯示只有 45~70px，= **5~10 倍縮小**，Bilinear 只取 4 個 texel、無 mipmap → 高頻噪點全部漏進畫面 | 原圖縮到顯示尺寸的 ~2 倍（改 meta 的 maxTextureSize 即可） |

---

## 1. 問題一：不順暢（卡頓/judder）

### 證據
- `ProjectSettings/TimeManager.asset`：`Fixed Timestep: 0.02`（物理 50Hz）。
- `Player.prefab` / `Monster.prefab`：`m_Interpolate: 0`（內插關閉）。
- `PlayerController.cs:348`：移動走 `_rb.velocity`（FixedUpdate，本身正確）。
- 相機 `MapCameraController` 在 LateUpdate 用 SmoothDamp 跟 transform（正確）。

### 機制
物理 50Hz、渲染 60Hz → 每 6 幀就有 1 幀「物理沒更新」或「更新兩次」。內插關閉時角色位置直接吃物理步進 → 玩家/怪物以固定節奏抖動（約每秒 10 次）。**這個抖動跟 fps 高低無關**，所以解開 VSync 飆到 300fps 一樣不順。相機是平滑的、角色是抖的，兩者對比反而讓抖動更明顯。

### KVM / VSync 說明
KVM（ATEN，見 DISPLAY_SETTINGS.md）把輸出鎖 60Hz。60Hz 螢幕上：
- VSync 開：穩定 60fps、節奏均勻 → **正確選擇**。
- VSync 關：fps 數字好看，但螢幕每 16.7ms 只能顯示一張，多算的全丟掉還撕裂。
「fps 狂升還是不順」正是因為問題在 50Hz 物理抖動，不在輸出張數。

---

## 2. 問題二：世界畫面粗糙

### 關鍵數字
- 地磚原生 256px/格（管線硬性，`MapSpriteLoader`：ppu = 256/tileSize）。
- `tileSize = 1`、相機 `followViewHeightTiles = 10` → 畫面高 = 10 格。
- 1080p：1080 ÷ 10 = **一格只有 108 螢幕像素** → 素材被縮到 **0.42 倍**。
- `MapSpriteLoader.SceneFilterMode = FilterMode.Point`，`new Texture2D(2,2,RGBA32,false)` → **無 mipmap**。

### 機制
Point 濾波放大時是「像素風」；**縮小時是災難**——每個螢幕像素只隨機挑一個原圖像素、丟掉週邊 5 個，結果是噪點、斷線、鏡頭移動時整個畫面閃爍蠕動。這就是「粗糙、髒」的體感來源。4K 螢幕下是 0.84x 所以會好很多；Mac Retina Game view 接近 1:1 所以編輯器看不出來。

### 驗證方法（已內建！）
遊戲裡按 **F**（`SetSceneFilterMode` 切 Point/Bilinear）直接 A/B 對比。Bilinear 會明顯變乾淨（略軟）；加上 mipmap 後縮小取樣才完全正確。

### 附帶收益
相機 SmoothDamp 產生的子像素位置在 Point 濾波下會造成 texel 邊界蠕動；改 Bilinear 後這個閃爍也會消失。

---

## 3. 問題三：UI 髒／顆粒大

### 關鍵數字
| 資產 | 原圖 | 實際顯示（1080p） | 縮小倍率 |
|---|---|---|---|
| 物品 icon（`UI/Icons/Items/`） | 256px | InventoryPanel `ItemIconSize=70` × 面板 scale ≈ **45px** | ~5.7x |
| CloseBtn / DragIcon / PopupIcon 等 | 500px | ~48px | ~10x |
| 按鈕 LongBtn | 612×408 | ~200×130 | ~3x |

UI 用 Bilinear 但 `enableMipMap: 0`、`maxTextureSize: 2048`（不會被匯入器縮小）→ 5~10 倍縮小時 Bilinear 只取 2×2 texel，等於在 10×10 的區域裡亂抽 4 點 → 顆粒、髒邊。

### 結論：不用換風格
黑暗像素風本身沒問題，問題是「大圖硬塞小格子」。把原圖尺寸壓到顯示尺寸的 ~2 倍以內，同樣的圖會立刻變乾淨。最省力做法：**不動 PNG 檔，只改 .meta 的 `maxTextureSize`**（Unity 匯入器的縮圖品質很好）。

---

## 4. 問題四：資源尺寸規範

### 尺規基準
一格 = 1 world unit = **108 螢幕 px @1080p / 216 px @4K**。
規則：**世界內素材 = 佔幾格 × 256px**（保 4K 餘裕）；**UI 素材 = 顯示尺寸 × 2**。

| 類別 | 建議原圖尺寸 | 匯入 maxTextureSize | 備註 |
|---|---|---|---|
| 地磚 | 256/格（現制，勿動） | — | 管線硬性 |
| **整張背景圖（`Background/`）** | **每格 ≥128px**（寬 = 格數×128、高 = 格數×128；1080p 下 1:1 有餘裕） | 依尺寸（≤4096 一般夠） | **⚠ 2026-09-03 稽核：目前全部是 1448×1086 拉伸貼齊地圖，只有 45~94 px/格，是全畫面唯一被「放大」顯示的東西**——見下方〈4.1 整張背景圖的解析度〉 |
| 玩家/怪物（1~2 格） | 256~512 | 512 | 現行 500 上限 ✅ 合理 |
| Boss/邪佛/場景大物（3 格+） | 每格 256，上限 1024 | 1024 | 1024 以上無收益 |
| 子彈/飛行物（≤1 格） | 128~256 | 256 | |
| 擊中/發射特效 | 256；全螢幕級 512 | 512 | 序列幀多時尤其要控 |
| 物品/武器 icon | **128~192** | **128** | 現 256~500 過大 → 髒的主因 |
| UI 小按鈕/游標類 | 顯示尺寸×2（多為 96~128） | 128 | 現 500 過大 |
| UI 大按鈕/牌匾 | ~256~512 長邊 | 512 | |
| 面板背景（背包/倉庫板） | ≤ 顯示大小（~1100×1400 OK） | 2048 | |
| 立繪/頭像 | 顯示高度×2，長邊 ≤1024 | 1024 | |
| 劇情大圖/開場漫畫/Loading/標題 | 1920×1080（長邊 ≤2048） | 2048 | 開壓縮（大圖壓縮肉眼難辨，省 build 體積） |

壓縮原則：全螢幕大圖開 `textureCompression`；小 icon 縮到 128 後保持不壓縮（小圖壓縮瑕疵明顯、體積本來就小）。

### 4.1 整張背景圖的解析度（2026-09-03 補稽核）

本節第 2 節當年是以「256px/格地磚」稽核的；後來地圖改走**整張背景圖拉伸貼齊畫布**（見 [MapEditor_DESIGN.md](MapEditor_DESIGN.md) §4.6），
沒有人重新算過背景的每格像素數。實算結果：所有底圖都是 ChatGPT 的輸出尺寸 1448×1086（或同級），
標準 18×10 房只有 **80 px/格**、血狂之爭 **45~63 px/格**；1080p 跟隨模式一格是 108 螢幕 px ⇒ **背景被放大 1.35~2.4 倍顯示、變軟**，
而角色（約 124 px/格）與地上物（250~500 px/格）都是縮小顯示、銳利。「銳利的角色貼在一張軟掉的背景上」正是「角色像貼紙」的主因之一，
色彩處理救不了（見 [PROBLEMS.md](PROBLEMS.md) **E29**）。血狂之爭最明顯，正因它的地圖最大、px/格最低。

**規則**：整張背景圖 **每格至少 128px**（1080p 108 px/格有餘裕、1440p 接近 1:1；4K 若要 1:1 得 216/格，先不追）。
寬 = 地圖格數 × 128、高同理；**大於 40×30 格的地圖不要用單張背景**（11520×6400 這種尺寸記憶體與匯入都不實際），改走地磚管線或拆成多張。

> **2026-09-07 補充：這條的真正約束是「圖的總像素數」，不是格數本身。** 邪佛廣場後來把畫布從 90×50 改成 48×36（見 [PROGRESS.md](PROGRESS.md)），48×36 雖然超過 40×30，但因為格數少了，128px/格 只需要 6144×4608；實際用 5792×4344（121 px/格）＝ **25.2M 像素，與既有的 `Stage_BloodFang_Diningroom`（3764×6688）完全同量級**，記憶體 128MB、`LoadImage` 解碼時間都在已驗證過的範圍內。**所以遇到 px/格 過低的大圖，第一個該問的是「這張圖真的需要這麼多格嗎」——縮格數比放大圖便宜得多**（廣場那次同時解決了「太空曠」與「太糊」兩個問題）。

現有地圖的現況（**2026-09-07 全表實測重量**，取代 2026-09-03 版；`px/格` 分 X/Y 兩軸列，
`變形` = 格數比 ÷ 圖的長寬比，**1.000 = 背景沒被拉扁**，換算規則見下方 §4.2）：

| ID | 地圖 | 格數 | 目前底圖 | px/格 X | px/格 Y | 變形 | 狀態 |
|---|---|---|---|---|---|---|---|
| 1~7, 9~11, 13~16 | 紅嫁衣各房／初始洞窟／初始森林／競技場 | 18×10 | 1448×1086 | 80.4 | 108.6 | **1.350** | ❌ 解析度不足＋格數比錯（4:3 的圖配 1.8 的格數）。零變形上限只有 **8×6**；要維持 18×10 得先把底圖放大到 4:3 以上並改格數 |
| 8 | RedBridalGown_LivingRoom2 | 10×18 | 941×1672 | 94.1 | 92.9 | **0.987** | ⚠ 兩個問題疊在一起：9:16 的圖配 10×18 不在階梯上（1.29% 壓縮），且 px/格 只有 93。9:16 階梯最低階 9×16 也才 104.6 px/格 ⇒ **先換底圖（≥1280×2304）再談格數** |
| 12 | Main_Square | 48×36 | 5792×4344 | 120.7 | 120.7 | **1.000** | ✅ 2026-09-07 完成（縮格數＋4x 放大，見 [PROGRESS.md](PROGRESS.md)） |
| 17 | BloodFang_InitialScene | 24×27 | 2880×3840 | 120.0 | 142.2 | **1.185** | ⚠ 底圖已換足（3:4），但格數比沒跟上 → 建議 **21×28** |
| 18 | BloodFang_GuessLobby | 23×20 | 3200×2400 | 139.1 | 120.0 | **0.862** | ⚠ 底圖已換足（4:3），格數比沒跟上 → 建議 **24×18** |
| 19 | BloodFang_Gallery | 15×25 | 1800×3001 | 120.0 | 120.0 | **1.000** | ✅ 比例正確（3:5），px/格 120 略低於 128 但可接受 |
| 20 | BloodFang_MainLobby | 31×25 | 3750×3001 | 121.0 | 120.0 | **0.992** | ⚠ 5:4 的圖，但 31×25 不在階梯上（0.77% 壓縮）→ 建議 **30×24**（125 px/格，只縮 1 格寬 1 格高） |
| 21 | BloodFang_BrewingRoom | 24×15 | 2274×1416 | 94.8 | 94.4 | 0.996 | ✅ 格數已在 8:5 階梯上（8×3／5×3），殘留的 0.37% 是**圖本身不是精確 8:5**、改格數解不掉；問題在 px/格 只有 94.6 → 要**換底圖**不是改格數 |
| 22 | BloodFang_Diningroom | 15×27 | 3764×6688 | 250.9 | 247.7 | **0.987** | ⚠ 解析度最充裕的一張（px/格 250），但 9:16 的圖配 15×27 不在階梯上（1.29% 壓縮）→ 建議 **18×32**（27×48 會大 3 倍，不建議） |
| 23 | BloodFang_Garden | 23×20 | 3200×2400 | 139.1 | 120.0 | **0.862** | ⚠ 同 18 → 建議 **24×18** |
| 24 | BloodFang_ArmoryRoom | 23×20 | 3200×2400 | 139.1 | 120.0 | **0.862** | ⚠ 同 18 → 建議 **24×18** |
| 25 | BloodFang_TrainingGround | 18×21 | 2508×2508 | 139.3 | 119.4 | **0.857** | ⚠ 正方形底圖配 0.857 的格數 → 建議 **19×19** |
| 26 | BloodFang_GrandCamp | 21×18 | 2896×2172 | 137.9 | 120.7 | **0.875** | ⚠ 4:3 底圖 → 建議 **20×15** |

> **2026-09-07 稽核結論**：§4.1 當初只盯「px/格 夠不夠」，漏了**格數比要等於圖的長寬比**這條。
> 底圖換大之後，px/格 幾乎都已達標，卻有 **8 張因為格數比不在階梯上而變形**：
> 17／18／23／24／25／26 壓縮 12~14%（圓形地板會變成橢圓），20 MainLobby 0.77%、22 Diningroom 1.29%。
> **換底圖時要同時檢查格數比**——規則見 §4.2。
>
> **判準不是「變形數字看起來夠小」，而是「格數在不在階梯上」**：階梯上的點變形必然 <0.1%，
> 所以只要看到 0.99x 這種數字，就代表格數不在階梯上（2026-09-07 修正：初版把 0.992 的 MainLobby
> 與 0.987 的 Diningroom 誤放行為「比例正確」，就是拿 ±1% 當門檻造成的）。

換圖方式：重產（ChatGPT 產不出這麼大就先產 1448 再用 AI 放大 2 倍；放大結果要保留手繪筆觸、不要銳化成塑膠感）或直接 AI 放大現有圖；
同名覆蓋原檔即可，`.dipanmap` 與可走層座標都不用動（背景是拉伸貼齊畫布的）。換完跑 Sync Map Assets。

**過渡期措施（已上線，換完背景要關）**：`Assets/Scripts/CharacterMipBias.cs` 對角色貼圖設 `mipMapBias = log2(角色px/格 ÷ 背景px/格)`，
讓角色的取樣密度先降到跟背景一樣，兩者在螢幕上一樣軟（代價是角色細節跟著掉一階）。遊戲中 P → **M** 可即時開關 A/B。
背景解析度提上來之後把 `CharacterMipBias.DefaultEnabled` 改 false（或整支刪除，掛點在 `MapLoader.BuildBackground/Teardown`、
`PlayerAnimator.Setup`、`MonsterAnimator.Setup`、`PerfHud`、`PlayModeStaticReset` 各幾行）。

### 4.2 背景解析度 ↔ 編輯器格數（換算基準，2026-09-07 定案）

**為什麼要有這條**：背景圖是**拉伸貼齊整個畫布**的（見 [MapEditor_DESIGN.md](MapEditor_DESIGN.md) §4.6），
不保持長寬比。所以「格數比」一旦不等於「圖的長寬比」，整張背景就被拉扁或拉高——
圓形地板變橢圓、方磚變菱形，而且**這件事在編輯器裡看不太出來**（畫布本來就照格數比顯示）。
§4.1 只管 px/格 夠不夠，這一節管的是**比例對不對**。兩條都要過。

#### 換算三步驟

給定背景圖 `W × H`：

1. **化簡長寬比**：把 `W:H` 化成最簡整數比 `a:b`（就近取常見比即可，誤差 <1% 都算同一個比）。
2. **算倍率**：`n = floor( W ÷ (128 × a) )`，至少為 1。
3. **格數 = `(a×n) × (b×n)`**。

`n` 就是「這張圖能切成幾階」——**`n` 越大地圖越大、每格像素越少**。步驟 2 用 128 是 §4.1 的下限；
若沿用專案實務的 120（廣場、Gallery、MainLobby 都落在這附近），把 128 換成 120 即可，通常會多出一階。

> **關鍵**：`a:b` 決定了合法格數只能是這個比的**整數倍**。4:3 的圖只能是 4×3、8×6、12×9、16×12、20×15、24×18、28×21…
> **中間的值（例如 23×20）一律會變形**，這正是血狂之爭那 5 張的成因。

#### 快查表（常見產圖尺寸）

| 背景解析度 | 來源 | 圖比 | **建議格數** | px/格 |
|---|---|---|---|---|
| 1448×1086 | ChatGPT 原生 | 4:3 | **8×6** | 181 |
| 2896×2172 | 原生 Upscayl 2x | 4:3 | **20×15** | 144.8 |
| 4344×3258 | 原生 3x | 4:3 | **32×24** | 135.8 |
| 5792×4344 | 原生 4x | 4:3 | **44×33**（120 底＝48×36） | 131.6 |
| 3200×2400 | 4:3 標準 | 4:3 | **24×18** | 133.3 |
| 2880×3840 | 3:4 直式 | 3:4 | **21×28** | 137.1 |
| 2048×2048 | Scenario 2K | 1:1 | **16×16** | 128.0 |
| 2508×2508 | 方形 | 1:1 | **19×19** | 132.0 |
| 3750×3001 | MainLobby | 5:4 | **25×20** | 150.0 |
| 1800×3001 | Gallery | 3:5 | **12×20** | 150.0 |
| 2274×1416 | BrewingRoom | 8:5 | **16×10** | 141.6 |
| 3764×6688 | Diningroom | 9:16 | **27×48** | 139.3 |

#### 常見比例的格數階梯（零變形，直接挑一階）

| 圖比 | 合法格數（由小到大） |
|---|---|
| 1:1 | 任意 `n×n` |
| 4:3 | 4×3、8×6、12×9、16×12、20×15、24×18、28×21、32×24、36×27、40×30、44×33、48×36 |
| 3:4 | 3×4、6×8、9×12、12×16、15×20、18×24、21×28、24×32 |
| 5:4 | 5×4、10×8、15×12、20×16、25×20、30×24 |
| 3:5 | 3×5、6×10、9×15、12×20、15×25、18×30 |
| 8:5 | 8×5、16×10、24×15、32×20 |
| 9:16 | 9×16、18×32、27×48 |

#### 檢核（改完地圖或換完底圖都跑一次）

```
變形 = (格數寬 ÷ 格數高) ÷ (圖寬 ÷ 圖高)
```

**必須 = 1.000**。階梯上的點變形必然在 **±0.001** 內，所以**看到 0.99x 就代表格數不在階梯上**——
不要拿 ±0.01 當及格線放行（MainLobby 0.992、Diningroom 0.987 就是這樣被漏掉的）。
唯一的例外是**圖本身不是精確整數比**（例：BrewingRoom 2274×1416 = 1.60593 而非 8:5 = 1.6），
這種殘差改格數解不掉，要改就得改圖。

順便看 `px/格 = 圖寬 ÷ 格數寬`，理想 ≥128、可接受 ≥120、低於 100 就是 §4.1 講的「背景糊」。

#### 基準案例（實測，可直接拿來對照）

| 地圖 | 背景 | 格數 | px/格 | 變形 | 備註 |
|---|---|---|---|---|---|
| BloodFang_Gallery | 1800×3001 | 15×25 | 120.0 / 120.0 | 1.000 | ✅ 標準範例（3:5 的第 5 階） |
| Main_Square | 5792×4344 | 48×36 | 120.7 / 120.7 | 1.000 | ✅ 4:3 的第 12 階（以 120 為底） |
| BloodFang_MainLobby | 3750×3001 | 31×25 | 121.0 / 120.0 | **0.992** | ⚠ **易漏的類型**：5:4 的圖、格數也很像 5:4，但 31×25 不在階梯上 → 正解 **30×24** |
| BloodFang_GuessLobby | 3200×2400 | 23×20 | 139.1 / 120.0 | **0.862** | ❌ **反例**：4:3 的圖配 1.15 的格數，水平壓縮 13.8%。正解 24×18 |

> ⚠ **`BloodFang_GuessLobby` 一度被當成換算基準（作者 2026-09-07 提出），但它本身是反例**——
> 23×20 不在 4:3 的階梯上。以它反推會得到「X 軸除 139.1、Y 軸除 120」這種兩軸不一致的規則，
> 等於把 13.8% 的壓縮固化成專案標準。**基準請用本節公式，不要用這張圖。**



---

## 5. 修正清單（依優先序）

1. **開內插**：`Player.prefab`、`Monster.prefab` 的 Rigidbody2D `Interpolate = Interpolate`（m_Interpolate: 0→1）。→ 直接解決抖動。
2. **物理對齊 60Hz**：TimeManager `Fixed Timestep 0.02 → 0.01666667`。與 60Hz 螢幕同步，消除拍頻（開了內插後屬保險，成本為物理多跑 20%）。
3. **場景貼圖改 Bilinear + mipmap**：`MapSpriteLoader` 預設 `SceneFilterMode = Bilinear`；`new Texture2D(2,2,RGBA32,false)` 第三參數改 `true`（LoadImage 會自動生 mip chain；記憶體 +33%）。→ 解決世界畫面粗糙與移動閃爍。
4. **UI icon 縮尺寸**：批次改 `UI/Icons`、`UI/Common` 等 .meta 的 `maxTextureSize`（icon→128、按鈕→512），照 §4 表。→ 解決 UI 髒。
5. **VSync 保持開啟**（Ultra 檔已是 vSync=1），別為了 fps 數字關掉；上架前做玩家畫面設定時再開放選項（DISPLAY_SETTINGS.md 已規劃）。
6. （小項）`MonsterActuator.MoveTowards` 在 Update 路徑設 velocity——可運作，但建議搬到 FixedUpdate 節奏，非急迫。

### 驗證方式
- 修 1+2 後：走路盯角色本體（不是相機），抖動應消失；PerfHud（P）看幀時是否平穩。
- 修 3 前後：遊戲內按 F 即時對比。
- 修 4 後：開背包/彈窗看 icon 邊緣。

---

## 6. 修正結果（2026-07-05 實施）

| 項目 | 狀態 |
|---|---|
| ① Player/Monster 開 Interpolate＋Fixed Timestep 60Hz | ✅ 已實施，**實測卡頓明顯改善** |
| ② MapSpriteLoader 改 Bilinear＋mipmap | ✅ 已實施（F 鍵可切回 Point 對比） |
| ③ UI icon meta →128／按鈕→512＋開 mipmap | ✅ 已實施（PNG 未動） |
| 熱浪扭曲改低頻大波（Atmosphere 4/5/6） | ❌ 已還原——作者偏好原本高頻觀感，屬美術選擇非 bug |

補充發現：Main_Square（Map 12）特別粗糙的主因是 **Atmosphere=5 的熱浪扭曲**（每幀 ±2~3px 高頻重取樣整個畫面）疊加火雨（SceneEffect=1），與地圖大小（90×50 格）無關——鏡頭跟隨模式固定只顯示 10 格高，texel 密度不隨地圖尺寸變。若日後仍嫌該圖粗糙，可調 shader 熱浪係數（`Atmosphere.shader` mode 4/5/6 段的 0.0014/0.0011）或關閉該圖氛圍做對比。

---

## 7. 一句話總結

不順 = 50Hz 物理 + 沒開內插；粗糙 = 256px 素材被 Point 濾波縮到 0.42x；UI 髒 = 5~10 倍縮小又沒 mipmap。**全部是縮放/取樣管線問題，風格不用換，素材不用重畫**（icon 只要調匯入尺寸）。
